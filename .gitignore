__pycache__/
*.pyc
.env
*.egg-info/
dist/
build/
.mypy_cache/
# Local SQLite (watchlist)
data/
# CA bundle regenerated from macOS keychain — never commit real certs
# certs/  # keep commented so Docker build COPY works; just don't commit secrets
